# Wiki (简体中文)

本目录为 Servy GitHub Wiki 的简体中文文稿源。

- 页面文件名保持英文（ASCII），以符合仓库 `wiki.yml` 校验。
- 由 `.github/workflows/sync-wiki.yml` 同步到仓库 Wiki。

首次启用 Wiki 时，请先在 GitHub 网页打开 `Wiki` 并创建任意首页，
然后再运行 `sync-wiki` 工作流（空 Wiki 尚无 `*.wiki.git` 远程仓库）。
